import { redirect } from 'next/navigation'

// La pàgina arrel redirigeix al dashboard.
// El middleware s'encarrega de redirigir al login si no hi ha sessió.
export default function Home() {
  redirect('/dashboard')
}
